import "@/index.css";
import React from "react";
import ReactDOM from "react-dom/client";
import Progre from "./Progress";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
      <Progre />
  </React.StrictMode>
);
